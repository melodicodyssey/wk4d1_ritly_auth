<div class="container">
<div class="jumbotron" style="background-color: rgba(250,250,250,10); color: black; border: 2px solid black">
    <h1>Welcome to Ritly!</h1>
    <p>With Ritly, you can easily compress the character count of your favorite website URLs!  Type/paste a URL into the Search box below.</p>
    <p>
    <%= form_tag ("ritly/create"), method: "post" do %>
        <%= text_field_tag (:orig_url)%>
        <%= submit_tag "Rit!", :class => "btn btn-lg btn-primary" %>
    <% end %>
    </p>
    <% if flash[:in_use] %>
    <p style="color: red"><%=flash[:in_use] %></p>
    <p style="color: green"><%=flash[:success]%></p>
    <% end %>
</div>
</div>

<div class="container">
</div>

<a href="/users/new">Sign Up</a>
<a href="sessions/new">Sign In</a>

