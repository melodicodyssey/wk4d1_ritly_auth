<h1>Sign In</h1>

<%= form_for (:session), sessions_path, do |f| %>
			<%= f.label :email %>
        	<%= f.text_field :email %>
        	<%= f.label :password %>
        	<%= f.password_field :password %>
        <% end %>
